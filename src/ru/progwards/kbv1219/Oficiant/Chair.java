/*
package ru.progwards.kbv1219.Oficiant;

import ru.progwards.kbv1219.Person.Person;

public class Chair {

    int id;
    Postiton postiton;
    double radius = 0.25 ; //radius in metr
    Person person;
    Plate plate;

    public Chair(int id) {

        this.id = id;
    }

    boolean inArea (Postiton otherPosition) {

        return postiton.inRadius(otherPosition,radius);
    }

    boolean isEmpty () {
        return Detector.chairIsEmpty(id);

        void detectPerson() {
            double personOnChairWeight = Detector.personOnChairWeight(id);
            if(personOnChairWeight < 30)
                //ребенок
                person = new Person (PersonType.CHILD, personOnChairWeight);
            else if (personOnChairWeight > 50)
                //папа
                person = new Person(PersonType.FATHER, personOnChairWeight);
            else
                //мама
        person = new Person (PersonType.MOTHER , personOnChairWeight);

        }
    }

*/
